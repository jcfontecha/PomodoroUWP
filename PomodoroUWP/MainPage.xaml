<Page
    x:Class="PomodoroUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PomodoroUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:PomodoroUWP.ViewModels"
    mc:Ignorable="d">
    <Page.Resources>
        <vm:TimerViewModel x:Key="TimerViewModel"/>
    </Page.Resources>

    <Grid Background="#FFE5A1A1" DataContext="{StaticResource TimerViewModel}">
        <TextBlock x:Name="textBlock" Margin="0,200,0,0" HorizontalAlignment="Center"
                   TextWrapping="Wrap" Text="{Binding Display}" VerticalAlignment="Top" Foreground="White" FontSize="24" Width="77"/>
        <Button x:Name="button" Content="Start" HorizontalAlignment="Center" Margin="0,314,0,0"
                VerticalAlignment="Top" Background="#66A90909" Command="{Binding StartTimerCommand}"/>

    </Grid>
</Page>
